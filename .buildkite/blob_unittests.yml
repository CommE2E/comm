steps:
  - label: ':nix: Blob Unittests'
    command:
      - './scripts/install_protobuf.sh'
      - 'cd services/blob'
      - 'cargo test'
    retry:
      automatic: true
    plugins:
      - docker#v5.3.0:
          image: 'rust:1.74'
          propagate-environment: true
    agents:
      - 'autoscaling=true'
