// @flow

export type VerticalBounds = $ReadOnly<{
  +y: number,
  +height: number,
}>;

export type LayoutCoordinates = $ReadOnly<{
  +x: number,
  +y: number,
  +width: number,
  +height: number,
}>;
